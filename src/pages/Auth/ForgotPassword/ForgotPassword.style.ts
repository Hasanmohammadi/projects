import { Box, styled } from '@mui/material';

const ForgotPasswordStyledContainer = styled(Box)`
  background: linear-gradient(
      0deg,
      rgba(255, 255, 255, 0.12),
      rgba(255, 255, 255, 0.12)
    ),
    linear-gradient(360deg, #d05d18 0%, #fd995d 89.48%);
  width: 100vw;
  height: 100vh;
  display: flex;
` as typeof Box;

export default ForgotPasswordStyledContainer;
