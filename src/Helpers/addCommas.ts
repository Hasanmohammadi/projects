const addCommas = (number: number): string => {
  console.log(
    '🚀 ~ file: addCommas.ts:2 ~ addCommas ~ number:',
    number?.toLocaleString(),
  );

  return number?.toLocaleString();
};

export default addCommas;
